# Session 46 — 2026-09-03 night — desktop (Claude Code)

Worktree `hermes-ecosystem-46-6f9bb3` (docs) / GAS on `hermes-wave-1-trust-94a9fb`.
`alfred_build_log` rows 88+.

## 1. /remind 403 — the third diagnosis is the one with evidence

| Session | Claim | What actually settled it |
|---|---|---|
| 45 (10:03) | consent granted view-only | — |
| 45 (21:40) | Calendar API disabled in project 884258367325 | Lloyd cannot even open that project |
| **46 (21:40)** | **n8n's OAuth client is in project NAVI-LLOYD, Calendar API not enabled there** | Client ID prefix `499022057812` read off the credential in the n8n UI; the console for that project shows **Enable** |

Retest: `/remind test ping in 5m` sent by the agent from the Telegram web tab (Lloyd's
instruction) at 21:36 PHT → exec **21083**, `Create Calendar Event` → `Forbidden`, card
"CALENDAR REFUSED · Nothing was created". No event exists to delete.

Why the earlier readings were wrong: Google's linked-apps page names the consent-screen app,
not the project; and the Cloud console silently falls back to the last-used project
(Alfred-Financial-Tower, API enabled, zero traffic) when `?project=` is inaccessible.

## 2. Overnight / scheduled runs (read-only checks)

- W-HERMES-NUDGE 12:30 → exec 21016, 19:30 → exec 21066, both success.
- W-DASH-SYNC execs 20933 (03:10) and 20938 (03:40) success. Report content not re-read.
- W-ERR: last fire 20848 (2026-09-02 08:31), nothing since → no WF-RAG-SEARCH error.
- W-DAILY-BRIEF: last run 20965 (07:00) — the YESTERDAY line and the Calendar Today node will
  be confirmed on the first 07:00 after the API is enabled.

## 3. Built (GAS, commit `880a271`, suite 94/94)

- `144_CategoryPass2.js` — second pass over `G2 Review / Uncategorized`. Evidence order:
  ledger merchant-majority (key = 119's normaliser, first four tokens; n≥2, share≥0.8; HIGH at
  n≥3) → `ALEX_MERCHANT_ROUTES` → blank. Marketplaces (`ALEX_MERCHANT_DEFER`) never get a
  majority suggestion. Worksheet `_ALEX_CategoryPass2` uses 130's header/grammar/engine;
  `catPass2AcceptHigh()` copies HIGH suggestions into DECISION; `categoryPass2Apply()` is
  `counterRulingsRun_(true)` on that tab. 10 checks in `_tests/alex_category_pass2.node.cjs`.
- `96_HermesStaging.js` — `hermesStagingInstallTrigger()` / `hermesStagingRemoveTrigger()`,
  daily 01:30 Asia/Manila (before W-DASH-SYNC 02:00).

## 4. Designed, not built

`drafts/AWAITING-STATEMENT-MATCHER.md` — enrich-only matcher for approved card captures.

## 5. Blocked on Lloyd

Enable Calendar API in NAVI-LLOYD · push/deploy v33 · run the pass-2 sequence · run
`hermesStagingInstallTrigger()` once · create `FMP apikey` credential · approve tg-328 / tg-333.

## 6. Not done

TEST-OmniRoute P2C webhook cleanup (E), Q4 HSBC name, `rag_query_rows` expression warning (F),
BotFather list — untouched.
