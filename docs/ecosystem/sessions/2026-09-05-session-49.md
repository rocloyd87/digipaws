# Session 49 — 2026-09-05 morning (desktop, Claude Code)

Boot per `CONTINUE.md` (session-49 prompt v2). Standing constraint unchanged: the 2026-08-28
anchors keep correlating; no rebuilds / merges / deletions / amount-date-account edits; counter /
category / label / note edits only through the snapshot-first engines. Build log rows 143–147.
GAS @68 → @73. Suite 110/110 (bare). Worktree `hermes-wave-1-trust-94a9fb` commits `2e410bb` …
`1c807a4` (pushed).

## 1. Balance gate — PASSED twice
`W-S48-READ` balance read at boot: latest `alexBalanceScheduled` 06:21:50, var 0.00 on all 8 FULL
accounts (BPI CC 62,269.48 · HSBC 0 · MAYA Black 529 · MAYA Landers 0 · RCBC Hexagon 8,806.19 ·
UB Visa 31,664.72 · BPI Main 167,610.09 · UB Debit 30,676.41); GCash COMPUTED_PARTIAL as known.
Re-run from the editor after the day's only ledger write: 06:49:58, var 0.00 on all 8. No restore.

## 2. Correlator audit — every applied decision read back by id
- `_ALEX_Correlator` holds only the 2026-09-04 20:47 preview rows (applied column blank because
  that run was a preview). The three decisions were applied at 21:33 PHT through
  `categoryPass2Apply` and read back: `gcash-671aa57c…` and `gcash-01bfe589…` carry
  counter=Tiktok Paylater (category blank, G2 label cleared); `alx-cb597be0…` (Jake Adame 85,000)
  carries A1 Debt Service > Personal Loan. The retracted self-counter (`gcash-1705d164…`, buy-load
  to own 9171348982) now reads C2 Connectivity > Family Internet & Load - Bohol from the buy-load
  rule — no self-counter in the ledger, nothing to retract.
- The 06:31:28 `correlatorScheduled` run (AUTO_APPLY on) logged zero decisions.
- **Finding:** `correlatorRun_` rebuilt each e-mail set from `tab` + `defaultAccount` only, so the
  live Amazon_Orders set lost `cardMatch` / `dayWindow` / `verdictFrom` and was matched like a BPI
  tab (own account, ±3 d). That is why every order came back "unmatched ±3d" on 2026-09-05 05:52
  while the harness (which builds sets by hand) passed. Nothing was lost — 170 had already ruled
  the Amazon rows by hand — but the card-side matching had never run live.

## 3. 133-style counter-id backfill (173) — APPLIED, 29 rows
`173_CounterIdBackfill` (runner `session49RunCounterIdBackfill`, snapshot `20260905064146`):
- Resolution order: the paired leg named in `EXPORT_HOLD:COUNTER_LEG_OF:<alex_id>` is the counter
  (its own `account_id` / `account_name`), written only when the counter NAME agrees with that leg
  (`CIB_NAME_ALIAS`: "BPI" / "VYBE" = BPI Main Account per 131 R2/R3; "Union Bank" = either UB
  account); a name-only transfer without a leg resolves through the ledger's own unique
  `account_name → account_id`; expense / income rows whose "counter" is the recipient's institution
  are reported, never written (an id would turn spending into a transfer leg).
- Written: 16 "BPI" legs + 10 "VYBE" legs + 1 "BPI Main Account" leg (all → `ms0q9gbnb7dcv3`,
  name normalised to BPI Main Account) + 2 OWNER_CAPTURES_20260828 legs by exact name.
- Read-back: `q=nameonly` 41 → 12; composition `transfer|blank|counter` 377 → 406 and
  `transfer|blank|nocounter` 34 → 5 (exactly +29 / −29); ledger rows 5,347 unchanged; five sample
  ids read counter=BPI Main Account.
- The 12 left (reported): 4 transfers whose name disagrees with the paired leg (`gcash-81b27d…`
  "CIMB GSave" vs leg on Union Bank Debit; `gcash-1941b1…` "BPI" vs leg on UB Visa; `gcash-c7ecdc…`
  / `gcash-cb43b3…` "Union Bank" vs legs on Cash; `gcash-df11c8…` "Union Bank" vs leg on BPI Main)
  and 8 GCash source-merge expense / income rows (→ Maya / Union Bank / BPI as recipient). Lloyd's
  call, if ever.

## 4. Correlator: split orders + truthful window (159) — DEPLOYED @68/@69
- Sum-of-charges: when no single card row matches an order total, exactly one same-card pair
  inside the window whose absolute cents sum to [total, total + 1.5 %] is that order
  (3,124.09 = 1,301.65 + 1,822.44, RCBC 2026-08-03/04); the open members take the item-class
  verdict with via `email:Amazon_Orders:split`; several pairs → ambiguous (candidates `id+id`);
  both members already ruled → silent.
- Unmatched reason quotes the set's own window: `±3d` for the symmetric default, `-3..+10d` for
  the Amazon set (`correlatorWindowText_`).
- `correlatorSet_` carries the config entry through to the plan (the §2 finding) — test added.
- Live `correlatorPreview` on @69 surfaced a false ambiguity: order 1,287.33 matched three pairs of
  unrelated small BPI-card rows (oona + petron, oona + wilcon, petron + …) that happen to sum inside
  the 1.5 % tolerance. @70: split pairs must both name the merchant (`/amazon|amzn/i` when the
  verdict comes from an order); test added. Preview on @70 (06:53:50): outflow 0, inflow 0,
  ambiguous [], unmatched = the BPI_Online / UnionBank_Online legs only (`±3d`, statements not yet
  ingested); every Amazon order is consumed silently (already ruled by 170).

## 5. Pending rulings — Lloyd answered 07:10, APPLIED (174, GAS @71-@73)
Lloyd: "calong calong and ann rey cerillo are all loaned amounts to other person... just make a
note of it"; "banknet, pos and gcash defer, i dont have enough context"; "its a gift" (keyboard).
- Keyboard rows `alx-66ec4b45…` (10,015.26) and `alx-1d94cb3b…` (3,335.95): D2 Gadgets → **E1
  Family Support & Repairs** through 130 on the 174 tab; the first pass left the D2 sub label
  behind (130 wrote the category but never cleared a sub when the new ruling has none) — 130
  patched (@72, "sub cleared") and a one-off 174 step cleared the two rows (@73). Read back: E1,
  sub empty, `ctx: gift for Kuya Joshua` kept.
- Calong Calong 23,000: stays G2 (no category ruled); note now ends `ctx: loaned to Calong Calong
  and Ann Rey Cerillo (owner 2026-09-05: amounts lent to other persons)`; DEFER on the worksheet.
- Bancnet P2M 807, POS Debit 455, Batch Invitation 1,000: DEFER on the worksheet (the 07:07 rebuild
  put the two 2024 rows on the tab). Worksheet: 0 pending, 22 deferred. G2 open 22 / 309,457.
- Balance gate after the writes: 07:15:05, var 0.00 on all 8 FULL accounts.

### 5-old. What was asked
Calong Calong + Ann Rey Cerillo 23,000 (2025-03-11, Other Accounts — siblings say debt →
A1 > Personal Loan?); Bancnet P2M 807 (GCash 2025-12-30); POS Debit 455 (RCBC Hexa Debit
2024-03-14); Batch Invitation Payment 1,000 (GCash 2024-02-26, an EXPORT_HOLD leg of
`alx-28fb0ec2…`). The latter two are not on the pass-2 worksheet (pre-2025 rows) — a 147-style
fill needs them added or a direct 130 tab. Also open: was the Marifel keyboard a gift (E1)?

## 6. Read-back
Composition (5,347 rows): `expense|G2|nocounter` 22 (309,457 lifetime open G2), `income|blank|
nocounter` 127 (all tagged), `transfer|blank|counter` 406, `transfer|blank|nocounter` 5.

## Lessons
- A harness that builds its fixtures by hand can pass while the live wiring drops the very fields
  the feature needs; when a live log contradicts a passing test, diff the run's input builder
  against the test's.
- The editor's function dropdown keeps the previous file's selection state; confirm the toolbar
  label before Run (one run went to `correlatorRetractSelfCounters` — harmless, 0/0).
