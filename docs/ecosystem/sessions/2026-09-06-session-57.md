# Session 57 — 2026-09-06 08:03 → ~09:20 PHT (desktop, Claude Code; persona phase 0 build)

Boot per CONTINUE.md session-57 prompt. Standing constraint unchanged: the 2026-08-28 anchors keep correlating; no
rebuilds / merges / deletions / amount-date-account edits. **GAS @76 unchanged** (worktree `hermes-wave-1-trust-94a9fb`
`2b40854`). No ledger write. No GAS change. Session worktree `hermes-alex-classification-48-33cc87` (nothing changed there;
the n8n sources for this session live in the workflows and in the Sunday n8n backup).

## Boot and gate
- `date` 08:03 PHT (00:03Z). The prompt was gated to after 20:05; Lloyd pasted it at 08:03, so the 11:06 / 12:30 / 18:00 /
  19:30 / 20:00 / Mon 08:00 checkpoints were still ahead. Balance gate PASSED (8 FULL var 0.00, verified 20:31:20).
- Executions since 07:45: routine (snapshot refresh, webhook guard, RAG ingest). One new error, exec 21773 08:00 =
  W-HERMES-DOCS "New Document in Drive" trigger poll ECONNRESET, zero items, transient; no fix.
- digipaws pulled (ec56d19).

## Phase 0 built (PRD v3.1 build order, D-137), all in n8n, no GAS change
| Piece | Where | Verified by |
|---|---|---|
| Shared narrow formatter | NEW sub-workflow **W-FMT-NARROW** `iK3doB7LPwuethmR` (Execute Workflow Trigger → Render Narrow → Width Row → Log Width → Return Render). Spec → Telegram HTML: chip on line 1, word-wrap, kv/peso (k-rounding above 9,999 unless `exact`), category/account aliases, `<pre>` tables max 4 rows + "+N more", inline-keyboard object, width audit. Every render logs one row to data table `hermes_fmt_width` `QFztSrySawucMmrh` | exec 21791 and every send below |
| House width | **38 chars** outside `<pre>` (PRD said 32; Lloyd at 08:45: "too narrow"); pre tables 1+20+1+12 (+3 flag) | width log rows max_line 37-38, width_ok true |
| W-DAILY-BRIEF | Format Brief = spec builder (POINT / CAUSE / IF NOTHING CHANGES / DO TODAY / TASKS / NEWS / TODAY) → Render Narrow → Send Brief | msg 385, exec 21792 |
| W-SCORECARD | Format Card weekly + monthly specs; buttons Trend/Subs (weekly), Goals/Merchants (monthly); Echo and Build Weekly Mail read the render | msg 384 (21790), msg 386 with buttons (21795) |
| W-HERMES-DIGEST | NEW Reshape Digest parses the GAS get_digest text (GAS unchanged) into the four blocks; goals / merchants / subs behind 3 buttons | msg 387 (21797), parsed:true |
| W-HERMES-NUDGE | NEW Reshape Nudge (findings[] → four blocks) → Render → Send Alert | msg 388 (21799) via a fixture hook, so no get_nudges call and the W36 cap untouched |
| W-HERMES /stats | Format Stats = exact-figure spec → Render Stats → Send Stats | msg 390 (21807) |
| Sentinel width check | W-SENTINEL: Run Checks → Read Width Log (width_ok=false rows) → Width Check (7-day window, fails result.ok, appends a WIDTH CHECK block) → Anything Failing? | structure validated; first real run Sun 18:00 |
| setMyCommands | home stats report pending sub note todo remind cascade alfred alexa robert status undo | Telegram `ok:true` (temp lane on W-S48-READ, removed) |
| Persona Router | W-HERMES Code node after Owner Allowlist: keyword table → persona (alfred/hermes/alexa/robert/custodian), chip, route; bare /alfred → /stats; IF chain Is Home → Is Status → Is Persona Card → Is Stats Command | execs 21803-21810 |
| /home | NEW **W-HOME-CARD** `llo1M5bZaU3mkCcf` (list_staged, get_attention, get_health, TickTick inbox, Drive 00 Inbox count, Drive Journal newest date; every source try/catch, n/a never 0) → Render → W-HERMES Send Home | msg 391 (21803): staged 0, inbox captures 7, parked 2, overdue 15, last journal none, weight 82 kg |
| /status | Fetch Ops Status → Format Status → Render → Send Status | msg 389 (21805) |
| /alexa /robert /undo | Persona Card (static phase-0 cards) | msg 392 (21810) |
| Chip on LLM replies | Reply text = `<b>chip</b>` + output | NOT verifiable from the simulator (Chat Memory keys on the Telegram Trigger) → Lloyd's next real message |
| Detail buttons | Parse Callback recognises `detail:*`; Detail? → Detail Config → Fetch Detail → Format Detail → Render → Send Detail (stateless re-fetch) | msgs 393-396 (21814/21816/21817/21819) |

## Lloyd feedback applied (08:45)
- Width 32 → 38 (renderer const, pre label 20 / value 12).
- Monthly scorecard: "▲ more than Jul, ▼ less" → "Biggest changes vs Jul: ▲ = this category cost more, ▼ = less"; POINT line
  "You spent less than in Jul."; CAUSE adds "Spend was N.Nx income." (only when income ≥ 10k, else "Almost no income landed").
- Digest / stats: "typical month is not net-positive — no ETA" → "A typical month spends more than it earns, so there is no
  date yet for net worth turning positive."; ONE MOVE capitalised.
- Renderer alias fallback: a long label is truncated with an ellipsis instead of cut to two words ("G1 Retire from seaf…").

## Gotchas found
- An Execute Workflow caller cannot be published while the sub-workflow is unpublished (`Cannot publish workflow ... not
  published`, automatic rollback). Activate the sub-workflow first.
- The Telegram node ignores a whole-collection expression for `inlineKeyboard`; rows must be static with per-field expressions.
- MCP `n8n_create_workflow` and `listTables` were blocked by the classifier once (the payload carried the GAS key); a temp
  lane on W-S48-READ and a key-free create went through.
- A simulator hook into Config exercises every deterministic lane and the callback lane, not the agent lane.

## Cleanup
Temp nodes removed: W-S48-READ probe + setcmds (back to 10 nodes), digest hook (6), nudge hook/config/fixture + Send Alert
chatId restored (7), W-HERMES sim (132 nodes). Build log rows 168 (session 56, never logged) and 169 (session 57).

## Checkpoints (session-51 list)
- PASSED before this session: 03:30 guardian (21724), 07:00 brief v2 (21762).
- Still ahead at close: 11:06 no SUCCESS mail; 12:30 / 19:30 nudges (four-block shape now; may report deferred); 18:00
  W-SCORECARD (new shape, buttons) + weekly mail + W-SENTINEL (with the width check); 20:00 API monitor; Mon 07:00 brief
  and 08:00 digest (new shape). Session 58 reads them.
