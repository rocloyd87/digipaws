# Session 51 — 2026-09-05 afternoon (desktop, Claude Code)

Boot per `CONTINUE.md` (session-51 prompt, Lloyd's directives D-130). Standing constraint unchanged: the
2026-08-28 anchors keep correlating; no rebuilds / merges / deletions / amount-date-account edits. GAS
**@73 → @75** (worktree `hermes-wave-1-trust-94a9fb` `1c807a4` → `b22a4d7`, modules 01–177, harness
111/111). Build log rows 153–155. Docs worktree: `hermes-ecosystem-46-6f9bb3`.

## 0. Balance gate — PASSED twice
08:24:04 run: var 0.00 on all 8 FULL accounts (read at boot and again after the 177 apply at 13:39).

## 1. Report consolidation + humanization (directive #1) — DONE, tested live
**Inventory (Gmail, last 7 d):** two mails every morning — `[Alfred Statements] SUCCESS` (32_Operations,
11:06 PHT, every night) and `Gmail Guardian - N processed` (a separate Apps Script project, 07:53). The
Shopee digest/guard/backfill mails exist in code but have no live trigger. Critical-only mails already
existed (32 failure/timeout, 78 ingest failure, W-WATCHDOG). Telegram reports: brief 07:00, nudges
12:30/19:30, Monday digest, Sunday review, monthly scorecard, W-DASH-SYNC 02:00 + verifier 03:40, W-ERR.

**Changes:**
- **32_Operations:** success → no mail (records `ALFRED_SYNC_LAST_SUCCESS_AT` + rolling 14-run list
  `ALFRED_SYNC_RUNS`); failure/timeout still mail, body humanized (title / sentence / WHAT TO DO / DETAIL).
  New `opsStatus_()` and `ops_status` tool.
- **Gmail Guardian `60_Digest.js`** (its own project, pushed): the 07:00 trigger stays; the function now
  mails only on Sunday (7-day window) or on a weekday with FAILED rows (critical); one sentence of context.
- **ONE weekly e-mail:** W-SCORECARD weekly branch → `Weekly mail?` → `Fetch Guardian` → `Build Weekly
  Mail` → Gmail. Test 13:27: "Your week in money · 5 Sep" received (thread `1a0700929c3734de`).
- **Critical channel:** W-ERR humanized + `Critical E-mail` Gmail node; NEW **W-GUARDIAN**
  `PFS3bVKzTwyJNmxm` ACTIVE 03:30 (Telegram when anything fired, Gmail only when critical; owner hook
  `GET /webhook/guardian-run`; test → 0 critical, 2 info).
- **Humanized (title / blank / one sentence / groups):** 97 digest (context sentence, ✅ ACTION moved to
  the top, goal dates as YYYY-MM-DD), 98 nudges (title, sentence, ₱ formatting), W-DAILY-BRIEF,
  W-SCORECARD weekly + monthly, W-DASH-SYNC run report / verifier / failure, W-WATCHDOG mail, W-ERR.
  Each read back through its owner hook (`get_digest`, `get_nudges`, scorecard-run, guardian-run,
  daily-brief-run exec 21546); the brief was seen in the Hermis chat at 13:35.

## 2. Brief v2 (directive #2) — DONE
W-DAILY-BRIEF `Cu6opCPfQPHJMKRJ` (8 → 13 nodes): `Fetch Subscriptions` (subscriptions_audit, ledger
only), `Fetch TickTick` (inbox `inbox132802495`, credential `TVu3QTaHN9dWC2Xy` verified: 17 open tasks,
15 overdue), `Fetch News` (Miniflux) → `News Prompt` → `Pick Top 5` (Gemini) → `Format Brief` rewritten:
sentence of context, MONEY, BUDGET LEFT, YESTERDAY, DUE ≤ 7 DAYS (recurring + subscriptions), TASKS
(overdue / today / soon), NEWS TOP 5 (links), TODAY. Every source non-fatal.

## 3. Miniflux on the VPS + KPI data quality (directive #3) — DONE
- **Miniflux** `/opt/miniflux` (miniflux/miniflux + postgres:16-alpine, `127.0.0.1:8085`, on
  `alfred_internal`; `.env` holds the generated passwords; admin `lloyd`). Categories PH Business /
  Maritime / PSE Tickers; 10 of 11 feeds parse (philstar → Cloudflare 403); 571 entries in 20 min.
  API key for n8n minted → credential `Miniflux API - alfred-brain` `6bWNG8kDuaNBtW2v`. No public
  hostname yet (n8n reaches `http://miniflux:8080` internally); `BASE_URL` is miniflux.rocloyd.com for
  when Lloyd adds the tunnel route.
- **budgetOutlook basis (93):** the median ran over a zero-padded 6-month series, so B3 = 273/mo
  (398, 0, 0, 148, 5017, 1074) and "worst first" meant smallest remaining. Now: median of PRESENT
  months, ≥ 3 of 6 present, worst = overspent → most used → biggest. Live: 14 budgets, total 165,318
  (79 % of burn; was 146,124), B3/E4/G1 no longer lead; `excludedRare` = E3 (2 mo), G2 (1), B1 (1).
  KPI cache fingerprint bumped to v5.
- **Guardian (176_KpiGuardian.js, `kpi_guardian` tool):** balance gate (critical), balance stale,
  statement sync (critical on a streak / 48 h), ingest alert (critical), budget vs burn (0.6–1.5),
  budget_rare, G2 share (warn > 25 %), blank categories, stale statements (45 d). Harness
  `alex_kpi_guardian.node.cjs` 8/8.

## 4. Housekeeping (directive #4)
- (a) **Blank-category rows: none.** 0 unpaired expense/statement_credit rows lack a category; the
  "30" were the 33 `balance_adjustment` rows (blank by design) + 4 counter-less credit_payments + 5
  transfers — nothing for 144/146.
- (b) **Name-only 12 → 7.** 177 (173 with `trustLeg`) filled the 5 EXPORT_HOLD legs from their paired
  leg (D-128): CIMB GSave→Union Bank Debit, BPI→UB Visa, Union Bank→Cash ×2, Union Bank→BPI Main
  (preview 5 / errors 0 → apply, snapshot `20260905133855`, read back by id). The 7 institution
  counters (Maya ×5, Union Bank, BPI on expense/income rows) stay reported.
- (c) `128_Own28SignFix.js` → `175_Own28SignFix.js` (git mv, pushed @74).
- (d) **Small accounts:** only GCash is PARTIAL (COMPUTED_PARTIAL; a fresh lifetime GCash export is the
  evidence — the current one stops 2026-08-04). Everything else non-FULL is `NO_STATEMENT_SOURCE`:
  Maya eWallet, Maribank, CIMB ×3, BPI Direct Saveup, BPI Checking, BPI Montero, RCBC Hexa Debit,
  Cash, Grab Pay, Shopee Pay, VYBE (a screenshot of today's balance flips each to a verified anchor),
  the UnionBank / Conrad / Northfield loans (schedule or latest SOA), Bybit / DragonFi / AREIT / BPI /
  MBT (app screenshots). Ask Lloyd only for screenshots.
- (e) **Sheet sharing:** "anyone with link · reader" is INHERITED from a parent folder (Drive API
  delete → 403). Lloyd restricts the folder.

## 5. LifeVault as capture target (directive #5) — bridge verified, mount pending
- Bridge peers: CouchDB `lifevault` ↔ `/data/files` (= `/opt/lifevault/files`). files→db VERIFIED
  (test `00-inbox/2026-09-05 bridge-test-s51.md` became a CouchDB doc in 12 s); db→files seen in the
  08-31 logs (phone notes saved to the mirror).
- W-INBOX-FILE: `LifeVault Bytes` → `Write LifeVault Note` (`/lifevault/00-inbox` or `10-journal`)
  runs in parallel with the Drive write (Drive = fallback for a week; Undo trashes the Drive copy only).
- **Blocked (classifier):** adding `/opt/lifevault/files:/lifevault` to the n8n service in
  `/opt/alfred/deploy/docker-compose.override.yml` + `docker compose up -d n8n`. Lloyd's command is in
  TASKLIST. Until then the LifeVault node errors silently and Drive keeps landing.
- W-OBSIDIAN-INGEST (02:45) already walks the mirror, so LifeVault captures get indexed with no
  W-RAG-INGEST change.

## 6. Statements / correlator
No September BPI/HSBC/UB statement yet; `_ALEX_Correlator` still holds the four 09-04 preview rows.
Nothing to optimize was observable (no run since 06:31).

## 7. Read-back (W-S48-READ)
5,347 rows: `transfer|blank|counter` 411 (was 406, +5 from 177), `expense|G2|nocounter` 22
(309,457), `income|blank|nocounter` 127 (tagged), name-only 7. Temporary helper nodes (GAS proxy,
TickTick probe, sheet restrict) removed from W-S48-READ.

## Also found
- **Watchtower on the VPS is in a restart loop** ("client version 1.25 is too old", Docker 29) — no
  auto-updates have run; fix = `DOCKER_API_VERSION=1.44` env on the watchtower stack (Portainer).
- `/spend` in W-HERMES still prints "(budgets = your own 6-mo category medians)"; the figures are the
  new basis, the caption is stale.

## Lessons
- Read the actual mailbox before consolidating: the "several-per-morning" e-mails were two senders,
  one of them a separate Apps Script project, not the Shopee modules the code suggested.
- The Bash tool unescapes backslashes in heredocs; write source with the Write tool or chr(92).
- New VPS stacks deploy through SSH; edits to a running stack's compose are blocked — hand to Lloyd.
